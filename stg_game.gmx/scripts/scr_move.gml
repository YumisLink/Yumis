if (keyboard_check(ord('W')) && T == 0)
    y -= ispeed;
    
if (keyboard_check(ord('S')) && T == 0)
    y += ispeed;
    
if (keyboard_check(ord('A')) && T == 0)
    x -= ispeed;
    
if (keyboard_check(ord('D')) && T == 0)
    x += ispeed;
