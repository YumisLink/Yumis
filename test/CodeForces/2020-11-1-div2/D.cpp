#include <cstdio>
#include <algorithm>
#include <cmath>
#define MAXN 500010
using namespace std;
bool cmp(const int a, const int b) { return a > b; }
int qwe, a[MAXN];
int main()
{
    scanf("%d", &qwe);
    for (int www = 1; www <= qwe * 2; www++)
        scanf("%d", &a[i]);
    sort(a+1,a+n+1);
    sort(a+n+2,a+n*2+1,cmp);
    int ans = 1;
    for (int i = 1; i <= n; i ++)
    {
        ans = ans * abs()
    }
}